# Tasks: Harden OAuth runtime configuration

> feature: harden-oauth-runtime-configuration

## T-269 — Harden OAuth runtime configuration and Gateway DPoP replay protection [concluida]
- Refs: US-142, US-143, AC-308, AC-309, AC-310
- Modelo: gpt-5.6-sol
- Esforço: alto
- Arquivos: package.json, pnpm-lock.yaml, infra/sst.config.ts, libs/identity/nest/src/better-auth/better-auth.factory.ts, libs/identity/nest/src/better-auth/better-auth.factory.spec.ts, libs/platform/nest/src/oauth-resource/oauth-resource.module.ts, libs/platform/nest/src/oauth-resource/oauth-resource.module.spec.ts, libs/platform/nest/src/oauth-resource/oauth-resource.types.ts, libs/platform/nest/src/oauth-resource/verification/oauth-resource.service.ts, libs/platform/nest/src/oauth-resource/verification/oauth-resource.service.spec.ts, libs/gateway/nest/src/auth/gateway-auth.module.ts, libs/gateway/nest/src/auth/gateway-auth.module.spec.ts, libs/gateway/nest/src/infrastructure/auth/dynamo-dpop-replay.store.ts, libs/gateway/nest/src/infrastructure/auth/dynamo-dpop-replay.store.spec.ts, test/harden-oauth-runtime-configuration.test.mjs, docs/evidence/harden-oauth-runtime-configuration/T-269.md
- Notas: Bounded context: Edge authentication plus the Identity issuer composition boundary. Use case: resolve OAuth configuration after Nest configuration loading and enforce production-safe Gateway DPoP verification. Aggregate: none; this is stateless request verification plus atomic, expiring replay reservations. Invariants: Identity and Gateway defaults match; configured issuer wins after `ConfigModule.forRoot`; the effective Gateway request URL uses the canonical public origin; one replay key is accepted at most once across instances; production fails closed without shared replay configuration; bearer authentication remains supported. Consistency boundary: one conditional DynamoDB write per DPoP proof key, shared by all Gateway instances. Affected ports: Better Auth `DpopReplayStore`; no business-domain port. Red: add focused tests for late configuration, aligned defaults, conditional replay reservation, replay-store propagation, and SST origin/table wiring before production edits. Green: add the minimum async module registration and DynamoDB adapter, then wire SST. Refactor: keep the focused Identity, Platform, Gateway, and deployment tests green. Verification is intentionally scoped to affected unit/integration/structural tests plus typecheck and lint for affected projects, followed by `onp-spec verify` and `onp-spec audit --ci`.
