# Owner: Apollo MCP integration-contract boundary
query getMyCart {
  cart {
    contents {
      nodes {
        key
        quantity
        product {
          node {
            id
            name
          }
        }
      }
      pageInfo {
        hasNextPage
        endCursor
      }
    }
  }
}
