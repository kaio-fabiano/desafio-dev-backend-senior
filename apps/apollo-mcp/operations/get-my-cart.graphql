query getMyCart {
  cart {
    contents {
      nodes {
        key
        quantity
        product {
          id
          name
        }
      }
      pageInfo {
        hasNextPage
        endCursor
      }
    }
  }
}
