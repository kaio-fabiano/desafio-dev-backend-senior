mutation removeFromCart($productId: ID!, $quantity: Int!) {
  removeFromCart(productId: $productId, quantity: $quantity) {
    id
    items { edges { node { quantity product { id name price } } } }
  }
}
