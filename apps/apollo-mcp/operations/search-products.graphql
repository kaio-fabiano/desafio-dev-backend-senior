query searchProducts($first: Int, $after: String) {
  products(first: $first, after: $after) {
    nodes {
      id
      name
      price
    }
    pageInfo {
      hasNextPage
      endCursor
    }
  }
}
