# T-202 gateway module boundary evidence

## Result

`GatewayAuthModule` now owns OAuth resource registration,
`TokenVerifierService`, and `AuthContextFactory` under the gateway `auth/`
folder. `GatewayModule` contains only Apollo Gateway composition and exports
`GatewayAuthModule`, preserving provider visibility for the application SSE
middleware.

No authentication, audience, scope, GraphQL routing, federation, or SSE
behavior changed.

## Red

Before the module extraction, the focused `AC-245` structural test failed for
the expected reason:

```text
ENOENT: no such file or directory, open
'libs/gateway/nest/src/auth/gateway-auth.module.ts'
```

## Green and refactor gates

| Gate                                 | Result                                                                                |
| ------------------------------------ | ------------------------------------------------------------------------------------- |
| AC-245 structural test               | PASS, 1 test                                                                          |
| Gateway library test target          | PASS, 35 Vitest tests and 2 Node acceptance tests                                     |
| Workspace Vitest coverage            | PASS, 260 tests; gateway source files 100% statements, branches, functions, and lines |
| Gateway build                        | PASS                                                                                  |
| Gateway typecheck and test typecheck | PASS                                                                                  |
| Gateway lint                         | PASS                                                                                  |
| `onp-spec verify`                    | PASS, 1/1 criterion with proof across 578 discovered tests                            |
| `onp-spec audit --ci`                | PASS, 195/195 acceptance criteria proved with no warnings                             |

The NestJS module spec imports `GatewayAuthModule` through a consumer module
and resolves both exported providers from the real Nest testing container.
