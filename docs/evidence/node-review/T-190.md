# T-190 — Node review integration

## Red

Before the remaining messaging and SSE lanes were integrated, the closure
contract was executed with:

```sh
NODE_ENV=test TSX_TSCONFIG_PATH=$PWD/tsconfig.base.json \
  node --import tsx --test --test-reporter=tap \
  test/resolve-node-review-inventory.test.mjs
```

It failed for the intended reason. Every unresolved ID belonged to T-188 or
T-189: NR-122–NR-150, NR-161–NR-165, and NR-086–NR-121 plus NR-128–NR-132.
The inventory had no disposition records before this test was introduced; the
contract now rejects an item that is pending, retains its original marker,
or lacks an existing maintained owner, behavioral test, or evidence record.

## Integration status

T-183 through T-189 now have explicit resolved dispositions for all 226
original markers. The AC-232 inventory contract passes with the original
wording preserved and each row linked to a maintained owner, evidence, and an
existing behavioral test. Final feature proof and global CI audit passed in the
root integration workspace; T-183 through T-190 are complete.

## Coverage integration

The reviewed gateway, identity, platform, and checkout production files are
included in Vitest coverage. Checkout is an idempotency and consistency
boundary, so its repository, service, command hash, and Woo adapter use the
critical 100% lines/statements/functions and 95% branch thresholds.

The first critical coverage run failed because `CheckoutService` had not
exercised its defensive missing-lease error and the Woo adapter had not proved
the missing-order reconciliation failure. Focused AC-229 tests now cover those
outcomes. The full current Vitest coverage run passed 212 tests. Checkout, inbox, outbox,
and saga consistency boundaries are all at 100% lines/statements/functions;
their minimum branch result is 96.66%. Messaging runtime lifecycle remains on
the general floor at 95.45% lines and 85.45% branches.

`AuthenticatedDataSource` is also a critical security boundary: it forwards
credentials only for trusted subgraph origins and controls whether a subgraph
response may set browser session state. Its initial full coverage result was
94.59% branches, with the absent-transport-header cases uncovered. The focused
test first established that a response without usable HTTP headers cannot
forward a session header; the full coverage run then reached 100% for all four
metrics on that file.

## Build integration

The three reviewed app build targets now emit JavaScript and rewrite relative
TypeScript import extensions. The tooling regression test verifies that build
does not use `--noEmit`; the actual Nx build produced each application's
`dist/out-tsc/apps/*/src/main.js`. Docker builds passed for all three apps from the monorepo root, including the
final SSE production source state.

The Nx cache metadata has an executable regression check for AC-225. Each app
declares its exact `dist/out-tsc/apps/<app>` output and includes the root
package manifest, lockfile, and TypeScript base configuration in its build
inputs. After a cache-seeding build, those three output directories were moved
aside and the same `nx run-many --target=build` command restored all three
`src/main.js` files from the local cache (6/6 cache hits). This protects the
emitted runtime modules, rather than treating a typecheck-only result as a
build artifact.

## SSE coverage integration

The final workflow GraphQL and SSE sources are included in the full coverage
run. `health.controller.ts` and `order-workflow.module.ts` use the general
floor; the authenticated-subject decorator, operations service, resolver,
replay adapter, subscription service, SSE handler, and middleware use the
critical 100% lines/statements/functions and 95% branch floor. Entry-point barrels, provider tokens, and type-only declarations are excluded
from instrumentation. A focused module composition test executes the
request-scoped factories and confirms that Nest owns `/graphql/stream`
middleware registration. The full coverage run passed 43 files and 243 tests;
all critical SSE sources reached 100% across all metrics, and the module
reached 100% statements and lines, 92.85% branches, and 90.9% functions.

## Historical contract migration

The final Node TAP run exposed three stale structural assertions after the
maintained Nest boundaries moved. Focused contract tests first failed for the
retired app-local gateway composition, the obsolete SSE request conversion,
and the old session-header shape. They now inspect the maintained gateway
module, its authenticated SSE context cache, and the allowlisted commerce
session forwarding behavior. The three focused files pass without removing
their acceptance criteria.

## Root typecheck and dependency cache correction

The first complete root TAP run passed 208 tests and failed four. Three failures
were the historical contract migrations above. AC-107 also failed because the
gateway application read stale declaration output from its referenced library:
`GatewayContext` existed in source but not in the old emitted index declaration.
The focused AC-107 test reproduced that failure before the configuration change.

Typecheck targets now build their project dependencies first. The three Nest
libraries declare their actual declaration-output directories and root compiler
and package inputs; app and library build hashes include dependency production
inputs. The same focused AC-107 test then passed without a manual prerequisite
build. A material cache check seeded the six app/library builds, moved their
generated output directories into a temporary backup, and restored every output
through six local cache hits. The restored gateway declaration includes the
current `GatewayContext` export. No user-authored source was removed by that
cache check.

## Test typechecking and IDE coverage

The final integration caught an invalid module-test assumption: the outbox uses
Nest's class provider, while the test required a factory. The corrected test
resolves the actual provider through a Nest testing module. No production
provider was changed to satisfy the test.

The app and gateway/identity library test projects were previously omitted from
TypeScript checks. Five test configurations and their Nx `test-typecheck`
targets now complement the existing platform target, and test targets depend
on them. The solution configurations expose all 43 owned spec files to the IDE
with decorators enabled. Test build-info paths are separate from production
build metadata. Compiler strictness was preserved; mock and fixture types were
corrected without changing production behavior.

The first typed checks reported genuine TS2345, TS2352, TS2304, TS2322, and
TS2722 diagnostics. All six uncached test-typecheck targets subsequently passed.
The root then independently ran `nx run-many -t typecheck,test-typecheck,lint,build`
for the three apps and three libraries with `--skip-nx-cache --parallel=1`:
all targets passed. A full coverage rerun passed 43 files and 243 tests at the
configured per-file floors. During this review, coverage caught two mock typing
edits that had changed missing-origin and scalar-cookie scenarios; both original
scenarios were restored with correct types before accepting the final result.

## Final traceability corrections

The read-only pre-audit found invalid historical feature status spellings,
unclaimed existing migration/broker/channel files, and one E2E annotation for
retired AC-083. Statuses now use the supported `implementada` value. The existing
migrations are explicitly mapped to checkout persistence (T-187), and the broker
and channel to the subscription boundary (T-189). AC-083 was retired by
`a2a37a3`; only that obsolete annotation was removed. The E2E assertions and their
valid AC-069/AC-084 references remain unchanged.

PostgreSQL container tests now generate their own ephemeral password and use it
for both container initialization and the ORM connection, satisfying the existing
no-literal-secrets audit without weakening its rule. Both real PostgreSQL suites
passed all 17 tests after that setup change. The gateway target contract now
includes the required `test-typecheck` target and its focused tests pass. The
read-only pre-audit reports only pending/stale verification records; no remaining
source, criterion, status, or principle findings remain.

## Build-test timing stability

Repeated TAP execution reproduced one integration-test timeout: the real Nx
build of three applications and their libraries occasionally exceeded Vitest's
five-second unit-test default. No runtime or artifact assertion failed. That
single test now allows 35 seconds, with its child process independently bounded
to 30 seconds. No latency or coverage budget was relaxed. Its focused eight-test
suite passed, followed by the complete TAP-reported Vitest coverage suite.

## Final acceptance

The final sequential verification executed the configured combined TAP command
for each of the 35 features. Every invocation completed with actual suite exit
code zero and every feature criterion passed; no saved test output was replayed.
The suites passed 212 Node tests and 243 Vitest tests across 43 files. The separate
full coverage run and all six uncached build, lint, production typecheck, and
test-typecheck projects passed, as did the three application Docker builds.

`onp-spec audit --ci` completed with zero warnings: 35 features, 85 user stories,
and all 182 acceptance criteria both tested and proved. The review's AC-225
through AC-232 all have passing proof in
[the verification record](../../../.spec/verification/resolve-node-review-todos.json).
The original 226 inventory markers have resolved dispositions; the final Node
production source scan has no TODO, FIXME, HACK, or XXX markers.

The original root branch and HEAD were preserved, with no staged changes.
Task changes remain in the working tree alongside the user's original edits.
