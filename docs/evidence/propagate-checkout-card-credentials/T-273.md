# T-273 — Propagate tokenized Card credentials

## DDD record

- Bounded contexts: Transaction, Inventory, and the Payment integration contract.
- Use case: start a tokenized Card checkout and request the matching provider payment after inventory reservation.
- Aggregates: `Transaction` and `InventoryReservation`; their existing transition invariants remain unchanged, and immutable payment-intent facts are carried by their events.
- Invariants: the exact short-lived provider token and payment-method identifier reach `RequestPayment`; Pix carries neither; PAN, CVV, expiry, and raw provider payloads remain absent; transaction and payment operation keys do not change.
- Consistency boundary: the event-sourced Transaction-to-Inventory flow and its transactional outboxes.
- Affected ports: `TransactionOutbox` and the existing AMQP integration contracts.

## TDD evidence

### Red

Command:

```text
node --test --test-name-pattern='AC-314|AC-315|AC-161' test/mercado-pago-payment-provider.spec.test.mjs
```

Result: exit 1; AC-161 and AC-315 passed, while AC-314 failed because `CheckoutService` did not pass `providerToken` and `paymentMethodId` to `StartTransaction`.

### Green and refactor

The existing command/event records were extended directly. `providerCredentialReference` remains the established AMQP token field, but it now contains the real short-lived provider token instead of the derived payment ID. The hardcoded `"card"` payment-method identifier was removed. No raw Card field, new dependency, or new abstraction was added.

Focused results:

- `CheckoutServiceTest`: `BUILD SUCCESSFUL`.
- `InventoryIntegrationEventHandlerTest`: `BUILD SUCCESSFUL`.
- `JpaTransactionPersistenceTest.staleProjectionEventsCannotRegressOwnerScopedViews`: `BUILD SUCCESSFUL`.
- `ChoreographedLifecycleE2ETest.inventoryFirstEventsReachPaymentOnlyThroughRabbitMqWithCausalMetadata`: `BUILD SUCCESSFUL`.
- `node --test --test-name-pattern='AC-161|AC-314|AC-315' test/mercado-pago-payment-provider.spec.test.mjs`: 3 passed, 0 failed.
- `node --test test/harden-oauth-runtime-configuration.test.mjs`: 2 passed, 0 failed; AC-313 passed with T-273 presented as resolved.
- Prettier check limited to the changed Markdown and Node files: passed.
- `git diff --check`: exit 0.

The focused Gradle checks exercised `compileTestJava`. Per task instructions, no full repository suite, `onp-spec verify`, or `onp-spec audit --ci` was run.
