export declare class FederationCapabilities {
  readonly bearer?: boolean;
  readonly origin?: string;
  readonly requestSession?: boolean;
  readonly responseSession?: boolean;
  readonly wordpressCredential?: boolean;
}
