export class OAuthCredentialMessages {
  static readonly invalidCredential = 'Invalid OAuth credential';
  static readonly invalidScope = 'Access token scope must be a string';
  static readonly invalidSubject =
    'Access token subject must be a non-empty string';
}
